
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class HomeScreenvet extends StatelessWidget {
  const HomeScreenvet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
         child:Text(
          "VET PAGE"
        ),
      ),
    );
  }
}
